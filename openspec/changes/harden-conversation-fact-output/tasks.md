## 1. 输出契约

- [x] 1.1 定义对话事实候选严格 JSON Schema，并用于模型请求。
- [x] 1.2 明确无候选时返回 `{"candidates":[]}`，并保证空数组正常完成处理。

## 2. 失败审计

- [x] 2.1 增加携带原始响应和请求元数据的事实提取错误。
- [x] 2.2 在标记 `fact_pending` 前保存失败 Trace，并按尝试次数生成独立 Trace ID。

## 3. 测试与验证

- [x] 3.1 测试严格 JSON Schema 请求和合法空候选结果。
- [x] 3.2 测试缺少 `candidates` 与非法 JSON 时保存失败原始响应。
- [x] 3.3 运行对话摄入定向测试和后端类型检查。

## 4. 数字与实体完整性

- [x] 4.1 强化消息级和 Session 级抽取约束，逐项生成数字、日期、时间和金额原子事实。
- [x] 4.2 禁止时间轴物化合并包含语义时间或数字细节的事实，并区分名称包含但不同的实体。
- [x] 4.3 在最终答题约束中要求问题实体与证据实体精确一致。
- [x] 4.4 增加定向测试并运行后端类型检查。

## 5. 相对月份语义

- [x] 5.1 将月份粒度的 relative point 归一化到目标月份月初，并从原始 source claim 恢复被摘要遗漏的时间锚点。
- [x] 5.2 将可由当前消息锚定的 `for N months` 持续状态保存为 valid-time 区间，并增加定向测试。
